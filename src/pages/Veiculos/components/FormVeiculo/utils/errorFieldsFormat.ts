const requiredField = 'Esse campo é requerido!';
const requiredFieldSelect = 'Selecione uma opção';
const dateTypeError = 'Preencha uma data válida';
const maxChars = (size: number): string => `No máximo ${size} caracteres`;

export { requiredField, requiredFieldSelect, dateTypeError, maxChars };
