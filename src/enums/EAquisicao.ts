export enum EFormaDeAquisicao {
  // não fazer nenhuma regra de negócio a partir desse enum: IRRELEVANTE. Pode deixar de existir um dia.
  COMPRA = '0',
  DOACAO = '1',
}

export enum EOrigemDeAquisicao {
  ORGANICO = '0',
  LOCADO = '1',
  CESSAO = '2',
}
